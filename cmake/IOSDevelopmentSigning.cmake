# Public identifiers used by Xcode automatic signing. Certificates, private
# keys, provisioning profiles, and Apple account credentials remain local to
# each developer machine and are intentionally not stored in the repository.
set(OPENWOW_IOS_DEFAULT_BUNDLE_IDENTIFIER "ink.mnt.elune")
set(OPENWOW_IOS_DEFAULT_DEVELOPMENT_TEAM "5TCGFUXXZP")
